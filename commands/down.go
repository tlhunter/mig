package commands

import "github.com/tlhunter/mig/config"

func CommandDown(cfg config.MigConfig) error {
	// If the migration to be run is "missing" then display an error.
	// Normally such a missing migration is acceptable but in this situation it's dire.

	return nil
}
